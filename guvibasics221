#include <stdio.h>

int main(void) {
	int N,fact;
	scanf("%d",&N);
      fact=factorial(N);
	printf("%d",fact);
  return 0;
}
int factorial(int N)
{
if(N==1)
return 1;
else
return N*factorial(N-1);
}
